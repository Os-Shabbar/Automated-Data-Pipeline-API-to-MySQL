# Core ETL dependencies
pandas>=2.0.0
numpy>=1.24.0
requests>=2.31.0
PyMySQL>=1.1.0

# Optional but recommended for loading variables from a local .env file
python-dotenv>=1.0.0

# Optional: needed in some MySQL 8 authentication setups
cryptography>=41.0.0
